import Button from '../../../components/button'

### useWeb3ModalAccount

Hook that returns the client's information.

```tsx
import { useWeb3ModalAccount } from '@reown/base/react'

function Components() {
  const { address, status, isConnected } = useWeb3ModalAccount()

  //...
}
```

### switchNetwork

```tsx
import { createWeb3Modal } from '@reown/base/react'
import { mainnet, arbitrum, polygon } from '@reown/base/chains'

const modal = createWeb3Modal({
  adapters: [wagmiAdapter],
  projectId,
  caipNetworks: [mainnet, arbitrum],
  metadata: metadata,
  features: {
    analytics: true,
  }
})

modal.switchNetwork(polygon)
```

### useWeb3ModalProvider

Hook that returns the `walletProvider` and the `WalletProviderType`.

```tsx
import { BrowserProvider } from 'ethers'
import { useWeb3ModalProvider } from '@reown/base/react'

function Components() {
  const { walletProvider } = useWeb3ModalProvider()

  async function onSignMessage() {
    const provider = new BrowserProvider(walletProvider)
    const signer = await provider.getSigner()
    const signature = await signer?.signMessage('Hello AppKit Ethers')
    console.log(signature)
  }

  return <button onClick={() => onSignMessage()}>Sign Message</button>
}
```

### getError

```ts
function Components() {
  const error = modal.getError();
  //...
}
```

<Button
  name="Learn More About Ethers"
  url="https://docs.ethers.org/v6/getting-started/#starting-blockchain"
/>
