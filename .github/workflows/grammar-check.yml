name: Grammar Check

on: [pull_request]

jobs:
  grammar-check:
    runs-on: ubuntu-latest

    steps:
      # Checkout the code
      - name: Checkout code
        uses: actions/checkout@v4

      # Run Vale using the latest GitHub Action
      - name: Grammar Check
        uses: errata-ai/vale-action@v2.1.1
